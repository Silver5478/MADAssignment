<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.edu.np.s10171537connect.madassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".start_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".start_gameActivity" />
        <activity android:name=".question_list_catergories" />
        <activity android:name=".question_select_onlinestatusActivity" />
        <activity android:name=".select_number_of_playerActivity" />
        <activity
            android:name=".question_ready"
            android:label="@string/title_activity_question_ready"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".enter_fake_answer" />
        <activity android:name=".select_answer" />
        <activity android:name=".show_answer" />
        <activity android:name=".question_score" />
        <activity android:name=".Enter_question_custom" />
        <activity android:name=".ScoreTaskViewHolder"></activity>
    </application>

</manifest>